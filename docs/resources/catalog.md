# Resource Catalog

The PyLabRobot Resource Library is rendered from the resource definitions used by PLR itself.
The catalog below is generated during the docs build: each item links back to its source page and opens a 3D preview from the same geometry metadata returned by `generate_geometry_catalog`.

```{raw} html
<div id="plr-labware-catalog"></div>
```
